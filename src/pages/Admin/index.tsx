
import { useNavigate } from 'react-router-dom';
import { Partner } from '../../components/Admin/Partner';

export const Admin = () => {
    return (
        <div>
            <Partner />
        </div>
    );
}